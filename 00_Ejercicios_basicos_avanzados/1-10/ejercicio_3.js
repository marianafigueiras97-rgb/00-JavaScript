// 3.1 Multiplica 10 por 5 y muestra el resultado mediante console.

const multiplicacion = (a,b)=> a * b;
console.log(`El resultado de multiplicar 10 por 5 es ${multiplicacion(10,5)}`);

// 3.2 Divide 10 por 2 y muestra el resultado en un console.

 const division = (a,b)=> a / b;
 console.log(`El resultado de dividir 10 entre 2  es ${division(10,2)}`);

// // 3.3 Muestra mediante un console el resto de dividir 15 por 9.

 const resto = (a,b) => a % b;
 console.log(`El resto de la division de 15 entre 9 es de ${resto(15,9)}`);

// // 3.4 Usa el correcto operador de asignación que resultará en o = 15,teniendo dos variables p = 10 y j = 5.
 const p = 10;
 const j = 5;
 let o = 0;
 console.log(`la variable p es igual a ${p}, la j es igual a ${j} y o es ${o}`);
 
 o += p+j;
 console.log(`el valor de o despues de la asignación es de ${o}`);
 
 // 3.5 Usa el correcto operador de asignación que resultará en i = 50,teniendo dos variables c  = 10 y m = 5.
 const c = 10;
 const m = 5;
 let i =0;
 
 console.log(`la variable c es igual a ${c}, la m es igual a ${m} e i es ${i}`);
 i += c*m;
 console.log(`el valor de i despues de la asignación es de ${i}`);
